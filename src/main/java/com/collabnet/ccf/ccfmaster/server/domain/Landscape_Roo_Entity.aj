// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.collabnet.ccf.ccfmaster.server.domain;

import com.collabnet.ccf.ccfmaster.server.domain.Landscape;
import java.lang.Integer;
import java.lang.Long;
import java.util.List;
import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.EntityManager;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;
import javax.persistence.PersistenceContext;
import javax.persistence.Version;
import org.springframework.transaction.annotation.Transactional;

privileged aspect Landscape_Roo_Entity {
    
    declare @type: Landscape: @Entity;
    
    @PersistenceContext
    transient EntityManager Landscape.entityManager;
    
    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    @Column(name = "id")
    private Long Landscape.id;
    
    @Version
    @Column(name = "version")
    private Integer Landscape.version;
    
    public Long Landscape.getId() {
        return this.id;
    }
    
    public void Landscape.setId(Long id) {
        this.id = id;
    }
    
    public Integer Landscape.getVersion() {
        return this.version;
    }
    
    public void Landscape.setVersion(Integer version) {
        this.version = version;
    }
    
    @Transactional
    public void Landscape.persist() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.persist(this);
    }
    
    @Transactional
    public void Landscape.remove() {
        if (this.entityManager == null) this.entityManager = entityManager();
        if (this.entityManager.contains(this)) {
            this.entityManager.remove(this);
        } else {
            Landscape attached = Landscape.findLandscape(this.id);
            this.entityManager.remove(attached);
        }
    }
    
    @Transactional
    public void Landscape.flush() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.flush();
    }
    
    @Transactional
    public void Landscape.clear() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.clear();
    }
    
    @Transactional
    public Landscape Landscape.merge() {
        if (this.entityManager == null) this.entityManager = entityManager();
        Landscape merged = this.entityManager.merge(this);
        this.entityManager.flush();
        return merged;
    }
    
    public static final EntityManager Landscape.entityManager() {
        EntityManager em = new Landscape().entityManager;
        if (em == null) throw new IllegalStateException("Entity manager has not been injected (is the Spring Aspects JAR configured as an AJC/AJDT aspects library?)");
        return em;
    }
    
    public static long Landscape.countLandscapes() {
        return entityManager().createQuery("SELECT COUNT(o) FROM Landscape o", Long.class).getSingleResult();
    }
    
    public static List<Landscape> Landscape.findAllLandscapes() {
        return entityManager().createQuery("SELECT o FROM Landscape o", Landscape.class).getResultList();
    }
    
    public static Landscape Landscape.findLandscape(Long id) {
        if (id == null) return null;
        return entityManager().find(Landscape.class, id);
    }
    
    public static List<Landscape> Landscape.findLandscapeEntries(int firstResult, int maxResults) {
        return entityManager().createQuery("SELECT o FROM Landscape o", Landscape.class).setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    }
    
}
